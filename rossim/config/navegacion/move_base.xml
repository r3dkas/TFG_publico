<launch>
    <!--node ns="local_costmap" name="voxel_grid_throttle" pkg="topic_tools" type="throttle" args="messages voxel_grid 3.0 voxel_grid_throttled" /-->
    <arg name="prefix" default="" doc="Prefix used for robot tf frames" /> <!-- used in the config files -->

    <node pkg="move_base" type="move_base" respawn="false" name="move_base_node" output="screen" clear_params="true">

        <rosparam file="$(find rossim)/config/navegacion/move_base_params.yaml" command="load" />

        <!-- global costmap params -->
        <rosparam file="$(find rossim)/config/navegacion/costmap_common_params.yaml" command="load" ns="global_costmap" />
        <rosparam file="$(find rossim)/config/navegacion/costmap_global_params.yaml" command="load" />
        <rosparam file="$(find rossim)/config/navegacion/costmap_global_params_plugins.yaml" command="load"/>
        <!-- local costmap params -->
        <rosparam file="$(find rossim)/config/navegacion/costmap_common_params.yaml" command="load" ns="local_costmap" />
        <rosparam file="$(find rossim)/config/navegacion/costmap_local_params.yaml" command="load" />
        <rosparam file="$(find rossim)/config/navegacion/costmap_local_params_plugins.yaml"  command="load" />
        <!--remap from="/map" to="/grid_map" /-->
        <remap from="map" to="/map" />
        <remap from="odom" to="/odom" />

    </node>
</launch>

  